---
title: Authors
layout: base.liquid
permalink: /authors/
---

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/">Home</a></li>
    <li class="breadcrumb-item active" aria-current="page">Authors</li>
  </ol>
</nav>

<h1>{{ title }}</h1>

{% assign sortedAuthors = authors | sort: 'lastName' %}

<div class="row row-cols-2 row-cols-md-3 g-3">
  {% for author in sortedAuthors %}
  <div class="col">
    <a href="/authors/{{ author.slug }}/" class="card h-100 text-decoration-none text-center">
      {% if author.photo != "" %}
      <img
        src="{{ author.photo }}"
        class="card-img-top p-3 rounded-circle"
        style="aspect-ratio: 1; object-fit: cover;"
        alt="{{ author.firstName }} {{ author.lastName }}"
      />
      {% endif %}
      <div class="card-body">
        <h5 class="card-title">{{ author.firstName }} {{ author.lastName }}</h5>
      </div>
    </a>
  </div>
  {% endfor %}
</div>
